name: concurrency
on: [push]
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true  
jobs:
  production-deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - run: sleep 15  
